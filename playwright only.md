//this can be used for desktop claude when using playwright to automate user actions via claude.

//claude_desktop_config.json

{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest"
      ]
        }
    }
}